name: Build kernels

on:
  push:
    paths:
      - ".github/workflows/kernel.yml"
  workflow_dispatch:
  schedule:
    - cron: "14 13 * * 5"

jobs:
  Read-configuration:
    name: 🐂 Parse *.config.json
    runs-on: ubuntu-latest
    outputs:
      CONFIGS: ${{ steps.generate-matrix.outputs.CONFIGS }}
      BUILD_DATE: ${{ steps.generate-builddate.outputs.BUILDDATE }}
    steps:
      - name: 😄 Checkout
        uses: actions/checkout@v4

      - name: 😆 Generate Matrix
        id: generate-matrix
        run: |
          echo "CONFIGS<<EOF" >> $GITHUB_OUTPUT
          jq -s '[.[][]]' Kernel/configs/*.config.json >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: ⏰ Set builddate
        id: generate-builddate
        run: echo "BUILDDATE=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT

  Build-Kernel:
    name: "🐎 Build kernel with SUSFS"
    runs-on: ubuntu-latest
    needs:
      - Read-configuration
    strategy:
      max-parallel: 4
      fail-fast: false
      matrix:
        CONFIG: ${{ fromJSON(needs.Read-configuration.outputs.CONFIGS) }}
    env:
      # Basic configuration
      WORKSPACE: ${{ github.workspace }}
      BUILD_DATE: "${{ needs.Read-configuration.outputs.BUILD_DATE }}"

      # Kernel source configuration (Update to Pixel 4 kernel repo)
      KERNEL_NAME: ${{ matrix.CONFIG.kernelSource.name }}
      KERNEL_REPO: "https://github.com/msnx/KernelSU-Pixel4XL"  # Update to your kernel repo
      KERNEL_BRANCH: "master"  # Adjust if you use a different branch
      KERNEL_DEVICE: "Pixel 4 XL"  # Your device name
      KERNEL_DEFCONFIG_PATH: "arch/arm64/configs/pixel4xl_defconfig"  # Path to your defconfig file

      # Enable SUSFS and KernelSU
      ENABLE_KERNELSU: true
      ENABLE_SUSFS: true  # Enabling SUSFS support

      # Whether to use ccache to speed up compilation
      ENABLE_CCACHE: ${{ matrix.CONFIG.enableCcache }}

      # Set output & ccache directory
      OUT_DIR: "${{ github.workspace }}/out"
      CCACHE_DIR: "${{ github.workspace }}/ccache"
    steps:
      # Set swap space
      - name: 📐 Set swap to 10G
        uses: pierotofy/set-swap-space@master
        with:
          swap-size-gb: 10

      - name: 😄 Checkout
        uses: actions/checkout@v4

      # Install prerequisites for Ubuntu
      - name: ⭐ Install prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y curl git wget libarchive-tools ccache python3

      # Clone the SUSFS repository and apply patches
      - name: 🌟 Clone SUSFS repository
        run: |
          git clone https://gitlab.com/simonpunk/susfs4ksu.git
          cd susfs4ksu

      # Apply SUSFS kernel patches
      - name: 💫 Apply SUSFS kernel patches
        run: |
          cd ${{ github.workspace }}/$KERNEL_NAME
          # Apply the patches required for SUSFS
          cp ${{ github.workspace }}/susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch $KERNEL_ROOT/KernelSU/
          cp ${{ github.workspace }}/susfs4ksu/kernel_patches/50_add_susfs_in_kernel.patch $KERNEL_ROOT/
          cp -r ${{ github.workspace }}/susfs4ksu/kernel_patches/fs/* $KERNEL_ROOT/fs/
          cp -r ${{ github.workspace }}/susfs4ksu/kernel_patches/include/linux/* $KERNEL_ROOT/include/linux/
          cd $KERNEL_ROOT/KernelSU && patch -p1 < 10_enable_susfs_for_ksu.patch
          cd $KERNEL_ROOT && patch -p1 < 50_add_susfs_in_kernel.patch

      # Enable SUSFS and KSU features in the kernel
      - name: ⚙️ Enable SUSFS and KSU features
        run: |
          cd ${{ github.workspace }}/$KERNEL_NAME
          # Ensure SUSFS and KernelSU options are enabled
          echo "CONFIG_KSU=y" >> .config
          echo "CONFIG_KSU_SUSFS=y" >> .config
          echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> .config
          make oldconfig

      # Build the kernel
      - name: 👍 Start building the kernel
        working-directory: ${{ env.KERNEL_NAME }}
        run: |
          make O=${{ env.OUT_DIR }} ARCH=arm64 -j$(nproc)

      # Check if Image exists in the output directory
      - name: 📂 Check if Image exists
        run: |
          echo "Checking if Image exists in the output directory"
          ls -la ${{ env.OUT_DIR }}/arch/arm64/boot/

      # Build and install ksu_susfs tool
      - name: 🛠️ Build ksu_susfs tool
        run: |
          cd ${{ github.workspace }}/susfs4ksu
          ./build_ksu_susfs_tool.sh

      # Upload kernel Image
      - name: 💾 Upload Image
        uses: actions/upload-artifact@v3
        with:
          name: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}-Image
          path: ${{ env.OUT_DIR }}/arch/arm64/boot/Image
          if-no-files-found: ignore
          retention-days: 7

      # Upload Image.gz (if exists)
      - name: 💙 Upload Image.gz
        uses: actions/upload-artifact@v3
        with:
          name: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}-Image.gz
          path: ${{ env.OUT_DIR }}/arch/arm64/boot/Image.gz
          if-no-files-found: ignore
          retention-days: 7

      # Upload dtb (if exists)
      - name: 💜 Upload dtb
        uses: actions/upload-artifact@v3
        with:
          name: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}-dtb
          path: ${{ env.OUT_DIR }}/arch/arm64/boot/dtb
          if-no-files-found: ignore
          retention-days: 7

      # Upload output directory
      - name: ☢️ Upload output directory
        uses: actions/upload-artifact@v3
        with:
          name: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}-Output
          path: ${{ env.OUT_DIR }}/arch/arm64/boot/
          retention-days: 7
